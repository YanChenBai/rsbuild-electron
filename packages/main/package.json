{
  "name": "main",
  "version": "0.0.1",
  "scripts": {
    "dev": "rsbuild build -w",
    "build": "rsbuild build",
    "analyze": "rspack build --analyze"
  },
  "dependencies": {
    "@electron-toolkit/utils": "^4.0.0"
  }
}
